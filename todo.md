* Test the actual behavior of #section-id links in combination with the
  slug/index.html structure and maybe aliases on a fork with GitHub pages.

* aliases for old ugly URLs

* fix css of footnotes

* add anchor links to section headings
  (enable Zola config, maybe adjust template)

* Upstream supports for sorting posts by path / permalink?
  Otherwise duplicate date in front matter and sort by that.

* Add scripts to:
  * download Zola (any platform)
  * start server
  * create new post
  (justfile?)
